import React from 'react';

const Title = () => (
  <div>
    <h1>Welcome to the Weather App!</h1>
    <p>Enter your location and see if its safe to go outside...</p>
  </div>
);

export default Title