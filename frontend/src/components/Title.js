import React from 'react';

const Title = () => (
  <div>
    <h1 className="title-container__title">Weather App!</h1>
    <h3 className="title-container__subtitle">Enter your location and see if its safe to go outside...</h3>
  </div>
);

export default Title